Bulletin Board 
This project was bootstrapped with [Create React App](https://github.com/facebookincubator/create-react-app).

This app adds,update, delete notes on bulletin-board.
Download or clone this project and do ng serve 
You can also build it and then run 
`npm run build` and then do `serve -s build`